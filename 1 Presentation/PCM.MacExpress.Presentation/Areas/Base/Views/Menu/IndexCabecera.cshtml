@model PCM.MacExpress.Presentation.Core.ViewModel.GeneralModels

<div class="navbar-custom-menu">
    <ul class="nav navbar-nav">
        <!-- Messages: style can be found in dropdown.less-->
        <li class="dropdown messages-menu">
            <!-- Menu toggle button -->
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                @*Municipalidad*@
                @*<i class="fa fa-envelope-o"></i>*@
                @*<span class="label label-success">4</span>*@
                <span>@Model.nombreEntidad</span>
            </a>
        </li>
        <!-- /.messages-menu -->
        <!-- Notifications Menu -->
        <li class="dropdown notifications-menu">
            @*<a href="#" class="dropdown-toggle" data-toggle="dropdown">
                    <i class="fa fa-bell-o"></i>
                    <span class="label label-warning">10</span>
                </a>*@
        </li>

        <!-- Tasks Menu -->
        <li class="dropdown tasks-menu">

            <!-- Menu Toggle Button -->
            @*<a href="#" class="dropdown-toggle" data-toggle="dropdown">
                    <i class="fa fa-flag-o"></i>
                    <span class="label label-danger">9</span>
                </a>*@
        </li>

        <!-- User Account Menu -->
        <li class="dropdown user user-menu">
            <!-- Menu Toggle Button -->
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <!-- The user image in the navbar-->
                <img src="~/Images/user2-160x160_1.png" class="user-image" alt="User Image" />
                <span class="hidden-xs">@Model.nombreUsuario</span>
                [<span class="hidden-xs">@Model.nombreRol</span>]
            </a>            

            <ul class="dropdown-menu">
                <!-- Menu Footer-->
                <li class="user-footer">
                    <div class="pull-left">
                        <a href="#" class="btn btn-default btn-flat">Cambiar contraseña</a>
                    </div>
                    <div class="pull-right">
                        <a href="#" class="btn btn-default btn-flat">Cerrar Sesión</a>
                    </div>
                </li>
            </ul>
        </li>

        <!-- Control Sidebar Toggle Button -->
        <li>
            <a class="btn btn-danger btn-sm"
               href="@Url.Action("Index", "Login")"
               title="Cerrar Sesión">
                <span class="glyphicon glyphicon-off"></span> Cerrar Sesión
            </a>
            @*<a href="#" data-toggle="control-sidebar">
                <i class="fas sign-out-alt"></i> Cerrar Sesión
            </a>*@
        </li>

    </ul>
</div>